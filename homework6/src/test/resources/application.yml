spring:
  datasource:
    url: jdbc:h2:mem:kermilov:bookdb
    data: test-data.sql
    initialization-mode: always
    show-sql: true
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none
  shell:
    interactive:
      enabled: false